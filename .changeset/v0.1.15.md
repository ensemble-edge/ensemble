---
"@ensemble-edge/ensemble": patch
---

- **Explicit Wrangler passthrough**: Use `ensemble wrangler <cmd>` for all Wrangler commands. Unknown commands now show helpful "Did you mean?" suggestions instead of silently failing.
