name: fetch-user
operation: code
handler: ./index.ts
description: Fetches user data (mock for demo purposes)

schema:
  input:
    userId: string?
  output:
    id: string
    name: string
    email: string
