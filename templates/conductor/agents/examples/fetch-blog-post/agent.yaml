name: fetch-blog-post
operation: code
handler: ./index.ts
description: Fetches a single blog post by slug from D1 database

schema:
  input:
    slug: string
  output:
    post: object?
    found: boolean
