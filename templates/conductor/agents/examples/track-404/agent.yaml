name: track-404
operation: code
handler: ./index.ts
description: Tracks 404 errors in D1 for analytics

schema:
  input:
    requestedPath: string?
    referrer: string?
    userAgent: string?
  output:
    tracked: boolean
